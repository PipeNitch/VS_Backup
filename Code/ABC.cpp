#include<bits/stdc++.h>
using namespace std;
#define windows ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)

bool comp(pair<char,short> a,pair<char,short>){
    char cfi,cse,cth;cin>>cfi>>cse>>cth;
    if()
}

int main(){windows;
    vector<pair<char,short>> a;
    short fi,se,th;cin>>fi>>se>>th;
    a.push_back({'A',fi});
    a.push_back({'B',se});
    a.push_back({'C',th});
    sort(a.begin(),a.end(),comp);

    return 0;
}