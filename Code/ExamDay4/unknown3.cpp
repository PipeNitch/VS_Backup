#include<bits/stdc++.h>
using namespace std;
#define windows ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)

int main(){windows;
    double a,b;cin>>a>>b;
    if(b>0)cout<<a+b;
    else cout<<a*b;

    return 0;
}