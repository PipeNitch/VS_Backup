#include<bits/stdc++.h>
using namespace std;
#define windows ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)

int main(){windows;
    string a,b;
    cin>>a>>b;
    cout<<(a==b?"True":"Wrong");

    return 0;
}